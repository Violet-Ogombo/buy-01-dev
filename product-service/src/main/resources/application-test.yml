spring:
  data:
    mongodb:
      uri: mongodb://mongodb:27017/buy01-test
  kafka:
    bootstrap-servers: kafka:9092
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
  instance:
    leaseRenewalIntervalInSeconds: 10
management:
  endpoints:
    web:
      exposure:
        include: health
